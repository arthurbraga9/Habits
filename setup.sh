# Install Homebrew if you missed it
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

brew install git gh            # git & GitHub CLI
python3 --version              # ≥ 3.9?
python3 -m pip install --upgrade pip virtualenv
